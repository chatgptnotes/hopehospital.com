node_modules/
.env
.env.local
.env.production
.DS_Store
dist/
build/
*.log
.vercel
.cache
coverage/

# Secrets and credentials
*.pem
*.key
.secrets
credentials.json
secrets.json

# Git credentials (IMPORTANT: Never commit tokens)
.git-credentials
.gitconfig.local

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
Thumbs.db
.Spotlight-V100
.Trashes

# Testing
.nyc_output/
test-results/

# Temporary files
tmp/
temp/
*.tmp
audit-screens/*.json
/himanshuhopehospt.*.pdf

# Local non-website assets (GBP post infographics, raw image sources)
gbp-assets/

# Raw GSC exports (keep the folder, ignore the data)
seo-system/data/*
!seo-system/data/.gitkeep

# Internal GBP post print sheets (kept local, not deployed)
gbp-posts-jun-01-07-2026.html

# Per-machine Claude Code settings (local only; untracked via `git rm --cached`)
.claude/settings.local.json

# GBP post draft graphics (already posted to Google Business Profile, not used on the site)
images/dr-murali-gbp-post*.png

# Generated daily SEO report exports (kept local — .md reports stay tracked)
seo-system/project-context/daily-reports/*.pdf
seo-system/project-context/daily-reports/*.html

# Raw GSC export archives (keep the .md insights tracked, ignore the zips)
seo-system/reports/*.zip
